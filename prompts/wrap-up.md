# Wrap-up

## Part 1 — Lessons
Summarize practical lessons from this task that will help me improve as:
- a developer
- a user of AI coding tools (Claude)
- a data engineer / analyst (if relevant)

Focus on:
- Workflow: what improved or slowed down the process?
- Mistakes: what caused rework, confusion, or inefficiency?
- Reusable patterns: what should I do again next time?
- Technical insights: only if directly useful
Keep it concise (max 4 bullet points). Be specific to this task. No generic advice. Categories above are suggestions — pick the most useful lessons regardless of category distribution.

## Part 2 — Rules update
Propose updates to your project's `CLAUDE.md` and `docs/spec.md`:

**Your project's CLAUDE.md** — only propose a change if at least one is true:
- A pattern was followed that isn't captured yet as a rule
- An existing rule was wrong or incomplete and caused a problem
- A rule was duplicated

**docs/spec.md** — only propose a change if at least one is true:
- A task was completed and is not yet marked done
- A section describes something that no longer matches what was built
- Something is factually incorrect

⚠️ Never write to `docs/spec.md` before the user has confirmed the proposed changes in the review gate below.

## Review gate

Do NOT write anything yet. Output the following for user review:

1. Proposed lessons (max 4 bullets)
2. Proposed CLAUDE.md changes (show exact additions, deletions, or edits)
3. Proposed spec.md changes (show exact additions, deletions, or edits)

Wait for explicit confirmation before writing any changes. Once confirmed:
- Append the lessons under a new task entry in `docs/lessons.md`
- If `docs/lessons.md` has a "Key patterns" section at the top, keep it and continue appending new task entries below it — do not remove any existing entries
- Apply the confirmed CLAUDE.md and spec.md changes. Keep both files concise and factual.
