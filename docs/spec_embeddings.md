# Spec: pgvector Embedding Pipeline

**Status:** Draft — awaiting approval  
**Date:** 2026-06-26  
**Model:** OpenAI text-embedding-3-small (1536 dimensions)

---

## 1. Product Brief

The scraper repo needs to generate and store a vector embedding for every active product in Postgres. These embeddings will power semantic search in the Next.js price tracker web app via pgvector cosine similarity. The web app handles query-time embedding; this repo handles per-product embedding generation and storage only.

**Scope of this spec:** schema change → embedding module → backfill script → discovery hook.  
**Out of scope:** query-time embedding, similarity search logic, web app changes.

---

## 2. Functional Requirements

| # | Requirement |
|---|---|
| FR-1 | Every active product must have an `embedding vector(1536)` stored in `products.embedding`. |
| FR-2 | Embeddings are generated using OpenAI `text-embedding-3-small` (1536 dimensions). |
| FR-3 | The embedding input is a plain-text string assembled from: `name`, `brand`, `category`, `ai_description` (if not null), and specs JSONB as `key: value` pairs. Price is deliberately excluded — see §5 "Why price is excluded". |
| FR-4 | Embeddings are generated at first product discovery (alongside `ai_description`), when `embedding IS NULL`. |
| FR-5 | Embeddings are **never regenerated** unless `embedding IS NULL`. |
| FR-6 | A standalone backfill script generates embeddings for all existing `active = true` products where `embedding IS NULL`. |
| FR-7 | If the OpenAI call fails, the product is skipped and logged — the failure must not block the scrape pipeline or discovery. |
| FR-8 | The backfill script supports `--limit N` (process first N products) and `--dry-run` (print embedding text without calling the API or writing to DB). |

---

## 3. Non-Functional Requirements

| # | Requirement |
|---|---|
| NFR-1 | Rate limiting: 100ms sleep between consecutive OpenAI API calls (both discovery hook and backfill). |
| NFR-2 | The discovery hook must add negligible latency to the discovery pipeline on API success, and zero latency impact on API failure beyond the single call timeout. |
| NFR-3 | All embedding logic is isolated in `src/embeddings.py` — no OpenAI calls in scripts or other modules. |
| NFR-4 | Unit tests cover text building, PG formatting, and error handling without live API calls. |
| NFR-5 | The backfill script logs `[OK]` / `[SKIP]` per product and prints a summary (total / processed / skipped) on completion. |
| NFR-6 | `OPENAI_API_KEY` must be present at runtime; the module should raise a clear error on import if the key is missing and the caller tries to generate an embedding. |

---

## 4. Technical Constraints

- **DB library:** `psycopg2` (synchronous) — consistent with the rest of `src/db.py`. No new DB library.
- **Embedding storage format:** the `[n1,n2,…]` string literal accepted by psycopg2 when writing to a `vector(1536)` column. No custom psycopg2 type adapter needed.
- **pgvector extension:** must be enabled on both local Postgres and Railway before the migration can run. Verify with `SELECT * FROM pg_available_extensions WHERE name = 'vector';` in both environments before T_E1.
- **OpenAI SDK:** `openai>=1.0.0` (the v1 client API). The same `OPENAI_API_KEY` already used by the Next.js web app.
- **ai_description dependency:** `ai_description` is generated by the planned Anthropic pipeline (spec_ai_descriptions.md, T27+) and does not exist yet. The embedding must include it **when not null** but must work correctly when it is null — both the discovery hook and backfill must handle the null case without error.
- **`store_embedding` lives in `src/db.py`, not `src/embeddings.py`:** it's a DB write, so it follows the same convention as `update_ai_description`/`update_ai_deal_description` — all DB writes live in `db.py`; `src/embeddings.py` is reserved for OpenAI-calling logic only (`build_embedding_text`, `generate_embedding`). `store_embedding` does not commit — see R-8.
- **Python version:** consistent with the existing project (3.10+).

---

## 5. Architecture Notes

### Embedding text format

Fields are included in this order; any field that is null, empty string, or missing is omitted:

```
{name}
Merk: {brand}
Categorie: {category}
{ai_description}
Specs:
{key}: {value}
{key}: {value}
…
```

**Specs serialisation:** iterate `specs` JSONB as key/value pairs; skip entries where value is `None`, `""`, or the literal string `"null"`. One `key: value` line per entry.

### Why price is excluded

Price changes daily and FR-5 says embeddings are never regenerated once set, so any price signal baked into the text would drift permanently — a product embedded as "premium, €450" stays that way in the vector even after it drops to a €180 deal, which is exactly the case this app most needs to surface well. Numeric price values also don't embed meaningfully (the model doesn't reason over magnitudes from tokenized digits). Price-based relevance/filtering is deferred entirely to the web app's query-time logic against `price_history`, kept separate from the vector. This also means neither the backfill query nor the discovery hook need to source `current_price` at all.

### Module: `src/embeddings.py`

Two functions, no side effects at import time (OpenAI-calling logic only):

| Function | Signature | Purpose |
|---|---|---|
| `build_embedding_text` | `(product: dict) -> str` | Assembles plain-text input string. Never raises. |
| `generate_embedding` | `(text: str) -> list[float] \| None` | Calls OpenAI API; returns None and logs WARNING on any failure. |

### Module: `src/db.py` (embedding-related additions)

| Function | Signature | Purpose |
|---|---|---|
| `format_embedding_for_pg` | `(embedding: list[float]) -> str` | Formats as `[n1,n2,…]` for psycopg2. |
| `store_embedding` | `(conn, product_id: int, embedding: list[float]) -> None` | Issues `UPDATE products SET embedding = %s WHERE id = %s`. **Does not commit** — matches `update_ai_description`'s pattern; caller decides commit strategy (see R-8). |

### Discovery hook placement

```
upsert_product()
  └─ [if T27+ done] write ai_description
  └─ [T_E5] if embedding IS NULL:
       fetch row → build text → generate → store
       on failure: log WARNING, continue
```

The hook queries `embedding IS NULL` after upsert so it is idempotent — re-running discovery never overwrites an existing embedding.

### Backfill query

```sql
SELECT id, name, brand, category, specs, ai_description
FROM products
WHERE embedding IS NULL AND active = true
ORDER BY id;
```

### ivfflat index

`lists = 100` is appropriate for a few hundred to low thousands of products and supports fast cosine similarity search. Can be re-created with a higher `lists` value if the product catalogue grows significantly (no data loss, just an index rebuild).

---

## 6. Implementation Phases

### Phase 1 — Foundation (no Python code yet)

1. Verify `pgvector` is available on local Postgres and Railway.
2. Add `openai>=1.0.0` to `requirements.txt`.
3. Add `OPENAI_API_KEY` to `.env.local` and Railway env vars.
4. Write and apply `sql/migrations/005_add_embedding_column.sql` locally, then Railway.
5. Update `sql/schema.sql`.

### Phase 2 — Core module

6. Implement `src/embeddings.py` (all four functions).
7. Write `tests/test_embeddings.py` (pure unit tests, no live API calls).

### Phase 3 — Discovery integration (moved ahead of backfill)

8. Add embedding hook to `scripts/discover_products.py`.
9. Test with `--limit 3` against local DB: confirm embeddings are generated for new products and not overwritten on re-run.
10. Deploy; confirm the hook populates `embedding` for at least one real newly-discovered product in production before proceeding to Phase 4.

### Phase 4 — Backfill (gated on Phase 3 production confirmation)

11. Implement `scripts/backfill_embeddings.py`.
12. Run dry-run + small `--limit` against local DB to validate end-to-end: embeddings written, format accepted by pgvector.

---

## 7. Risks / Ambiguities

| # | Risk / Ambiguity | Mitigation |
|---|---|---|
| R-1 | **pgvector not available on Railway.** Railway's managed Postgres may not have the extension pre-installed. | Run `SELECT * FROM pg_available_extensions WHERE name = 'vector';` in the Railway DB console before writing any migration code. Block T_E1 until confirmed. |
| R-2 | ~~`current_price` not on the products table.~~ **RESOLVED — no longer applicable.** Original concern: the backfill query and discovery hook would need to source the latest price from `price_history`. | Superseded: price is excluded from the embedding text entirely (see §5 "Why price is excluded"), so neither the backfill query nor the discovery hook need `current_price` at all. |
| R-3 | **`ai_description` not yet implemented (T27+).** Embeddings generated before T27+ will be missing the description field. | Acceptable — FR-5 says embeddings are never regenerated unless null. Once T27+ lands and descriptions are backfilled, a second embedding backfill pass can be run if richer embeddings are desired. Flag this as a conscious trade-off. |
| R-4 | **ivfflat index requires rows to exist before it is useful.** An index built on an empty (or near-empty) table has poor query performance until the catalogue is populated. | Apply the index in the migration regardless; it will become effective after the backfill run. No action needed beyond awareness. |
| R-5 | **Rate limit from OpenAI.** `text-embedding-3-small` has generous rate limits, but a large backfill (800+ products) could hit RPM limits. | 100ms sleep gives ~10 req/s, well within free-tier limits. If throttled, increase sleep to 500ms in `--limit` test runs before running full backfill. |
| R-6 | **Specs JSONB structure varies by category.** Key names differ across headphones, earbuds, speakers, soundbars. | `build_embedding_text` iterates whatever keys are present — no hardcoding. No special handling needed. |
| R-7 | **Sparse embeddings when `specs` (or `ai_description`) is empty at generation time.** Unlike price, this isn't a correctness bug — the data isn't wrong, just less rich (worst case: `name` + `Categorie:` only, since both are required by `validate_product_details`). Per FR-5, once generated it is never regenerated, so this gap is permanent unless deliberately fixed. | Accepted as-is, same treatment as R-3. If richer embeddings are wanted later: manually reset the affected rows with `UPDATE products SET embedding = NULL WHERE specs = '{}'::jsonb;`, then re-run the backfill script (scoped to `embedding IS NULL`) to regenerate them. |
| R-8 | **`store_embedding` does not commit.** Committing internally would break `discover_products.py`'s all-or-nothing per-category rollback (a Postgres commit finalizes everything pending in the transaction, not just the current row — an early commit from inside the loop would let earlier, still-uncommitted products in the same run survive a later `conn.rollback()` triggered by e.g. `CoolblueBlockedError`). | The discovery hook (T_E5) relies on `run_category`'s existing single end-of-batch commit — no change needed there. The backfill script (T_E4) is long-running and needs per-row durability instead (a crash partway through a large backfill shouldn't lose already-embedded rows), so it must call `conn.commit()` itself immediately after each `store_embedding()` call. |

---

## 8. Task Breakdown

| Task | Description | Status |
|---|---|---|
| T_E1 | Verify pgvector availability on local + Railway; write and apply schema migration | ✅ Done |
| T_E2 | Add `openai>=1.0.0` to `requirements.txt`; confirm `OPENAI_API_KEY` in both environments | ✅ Done |
| T_E3 | Implement `src/embeddings.py` (4 functions) and `tests/test_embeddings.py` | ✅ Done |
| T_E5 | Add embedding hook to `scripts/discover_products.py`; test with `--limit 3`, then confirm working in production | ⬜ Not started |
| T_E4 | Implement `scripts/backfill_embeddings.py`; validate with dry-run + small `--limit` against local DB. Full backfill runs against production only, after T_E5 is confirmed working in production | ⬜ Not started |

---

### T_E1 — Verify pgvector and apply schema migration

**Phase:** 1  
**Files:** `sql/migrations/005_add_embedding_column.sql`, `sql/migrations/rollback_005_add_embedding_column.sql`, `sql/schema.sql`

Steps:
1. Run `SELECT * FROM pg_available_extensions WHERE name = 'vector';` on local Postgres and Railway DB console. **Stop here if not available on Railway.**
2. Create `sql/migrations/005_add_embedding_column.sql`:
   ```sql
   CREATE EXTENSION IF NOT EXISTS vector;
   ALTER TABLE products ADD COLUMN IF NOT EXISTS embedding vector(1536);
   CREATE INDEX IF NOT EXISTS idx_products_embedding
     ON products USING ivfflat (embedding vector_cosine_ops) WITH (lists = 100);
   ```
3. Create `sql/migrations/rollback_005_add_embedding_column.sql`:
   ```sql
   ALTER TABLE products DROP COLUMN IF EXISTS embedding;
   ```
4. Apply locally via pgAdmin. Verify with `\d products`.
5. Apply on Railway.
6. Update `sql/schema.sql` to include the column and index.

---

### T_E2 — Add openai dependency and env var

**Phase:** 1  
**Files:** `requirements.txt`

1. Add `openai>=1.0.0` to `requirements.txt`.
2. Confirm `OPENAI_API_KEY` is set in `.env.local`.
3. Confirm `OPENAI_API_KEY` is set in Railway environment variables and shared/attached to the `discover_products` service (needed for the T_E5 discovery hook). The T_E4 backfill script is run manually against production via the existing `DATABASE_URL`-manual-prod pattern and reads `OPENAI_API_KEY` from local `.env.local` — no separate Railway service required.

---

### T_E3 — Implement `src/embeddings.py` and unit tests

**Phase:** 2  
**Files:** `src/embeddings.py` (create), `tests/test_embeddings.py` (create)

Implement all four functions as specified in §5. No OpenAI calls at import time.

*(Note: `format_embedding_for_pg` and `store_embedding` were later relocated to `src/db.py` during T_E5 — see R-8. `src/embeddings.py` now holds only `build_embedding_text` and `generate_embedding`.)*

Unit tests (no live API, no live DB):
- `build_embedding_text` with fully-populated product dict — assert all fields present in output
- `build_embedding_text` with all optional fields null — assert output contains at least `name`
- `build_embedding_text` skips specs entries with null/empty values
- `format_embedding_for_pg` produces correct `[n,n,…]` string
- `generate_embedding` returns None and logs at WARNING when `OpenAIError` is raised (mock the client)

---

### T_E5 — Add discovery hook in `scripts/discover_products.py`

**Phase:** 3  
**Files:** `scripts/discover_products.py` (modify), `src/db.py` (add `format_embedding_for_pg`, `store_embedding`)

1. After `upsert_product()` succeeds, add the hook:
   - Query `SELECT id, name, brand, category, specs, ai_description FROM products WHERE id = %s AND embedding IS NULL`.
   - If row returned: build text → generate → store.
   - If not returned (embedding already set): skip.
2. Wrap the entire hook in `try/except Exception`; log WARNING on failure, continue.
3. Sleep 100ms after each API call.
4. Test with `python scripts/discover_products.py --limit 3` against local DB: confirm embeddings generated for new products and not overwritten on re-run.
5. Deploy to Railway. Confirm at least one real newly-discovered product in production gets `embedding` populated by the hook before starting T_E4's backfill.

---

### T_E4 — Implement `scripts/backfill_embeddings.py`

**Phase:** 4 (gated on T_E5 being confirmed working in production)  
**Files:** `scripts/backfill_embeddings.py` (create)

1. Implement the script with `--limit N` and `--dry-run` flags. Module docstring must describe both flags.
2. Call `conn.commit()` immediately after each `store_embedding()` call (per-row durability for this long-running script — see R-8; `store_embedding` itself does not commit).
3. Run `python scripts/backfill_embeddings.py --dry-run --limit 5` against local DB — inspect printed embedding texts.
4. Run `python scripts/backfill_embeddings.py --limit 5` — inspect DB rows to confirm embeddings are stored and accepted by pgvector.
5. Do not run a full backfill against local DB. Once validated with `--limit 5`, run the full backfill against production. Local DB can be refreshed with a copy of production data afterward if a fully-embedded local dataset is needed for further testing.

---

## 9. Acceptance Checklist

### Schema
- [x] `pgvector` extension confirmed available on local Postgres
- [x] `pgvector` extension confirmed available on Railway Postgres
- [x] `005_add_embedding_column.sql` applied locally; `\d products` shows `embedding vector(1536)`
- [x] `005_add_embedding_column.sql` applied on Railway
- [x] `rollback_005_add_embedding_column.sql` created and reviewed
- [x] `sql/schema.sql` updated to include column and ivfflat index

### Dependency
- [x] `openai>=1.0.0` in `requirements.txt`
- [x] `OPENAI_API_KEY` set in `.env.local`
- [x] `OPENAI_API_KEY` set in Railway env vars, shared with the `discover_products` service

### Core module (`src/embeddings.py`)
- [x] `build_embedding_text` produces correct output for fully-populated product
- [x] `build_embedding_text` handles all-null optional fields without raising
- [x] `build_embedding_text` skips null/empty spec values
- [x] `generate_embedding` returns None and logs WARNING on OpenAI failure
- [x] `format_embedding_for_pg` produces `[n,n,…]` string
- [x] `store_embedding` issues correct UPDATE and commits
- [x] No OpenAI calls at import time
- [x] All unit tests pass (`pytest tests/test_embeddings.py`)

### Discovery hook (implemented and verified before backfill)
- [ ] `discover_products.py --limit 3` generates embeddings for newly discovered products
- [ ] Re-running discovery does not overwrite existing embeddings
- [ ] An OpenAI failure in the hook does not abort discovery (log only)
- [ ] No embedding-related import errors when running without `OPENAI_API_KEY` set (fail at call time, not import time)
- [ ] Confirmed in production: a real newly-discovered product gets `embedding` populated by the hook

### Backfill script (gated on the discovery hook check above)
- [ ] `--dry-run --limit 5` prints embedding texts without writing to DB
- [ ] `--limit 5` writes embeddings to DB; rows confirmed in pgAdmin
- [ ] pgvector accepts the stored format (no type error on write)
- [ ] Full backfill run against production completes without crashing; summary log shows 0 unexpected skips
- [ ] Re-running backfill on already-embedded products: no rows updated (query filters `embedding IS NULL`)
- [ ] Local DB refreshed from a production copy after the production backfill, if a fully-embedded local dataset is needed for further testing

### Deployment
- [ ] Railway cron job for discovery unchanged (no `--limit` flag in production)
- [ ] No changes to existing scrape_price_history pipeline
